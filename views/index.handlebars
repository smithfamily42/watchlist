<section class="pageSize">
    <div class="container px-0">
        <div class="row justify-content-center mx-0 mb-5">
            <div class="col-12 col-lg-6">
                <img src="/images/Watchlist-jumbo-logo.svg" alt="Watchlist Logo" class="img-fluid" />
            </div>
            <div class="col-lg-12 py-3">
                <h1 class="text-center sub-text">Your Personal Movie and Show "To-Do List"</h1>
            </div>
        </div>
        <div class="row d-flex justify-content-center gy-4">
            {{#if loggedIn}}
            {{else}}
            <div class="col-12 col-lg-4">
                <p class="text-center display-4 fs-4 px-3">Easily add and save movie and show data to your own personal list. Now you don't have to worry about forgetting the name of that show your friend told you to watch!</p>
            </div>
            <div class="col-12 col-lg-4 d-flex justify-content-center align-items-center">
                <button type="button" class="btn btn-primary px-5 py-3">
                    <a href="/login" class="text-white create-button">
                        Get Started!
                    </a>
                </button>
            </div>
            {{/if}}
        </div>
    </div>
</section>